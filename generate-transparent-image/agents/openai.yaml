interface:
  display_name: "Generate Transparent Image"
  short_description: "Create editable transparent PNGs from prompts or references"
  default_prompt: "Use $generate-transparent-image to create and preserve an editable transparent PNG from my prompt or reference image."
